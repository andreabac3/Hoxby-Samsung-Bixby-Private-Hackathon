capsule {
  id (viv.hok)
  version (0.1.1)
  format(3)
  targets {
    target (bixby-mobile-en-US)
  }
  capsule-imports {
    import (viv.core) { as (core) }
    import (viv.common) { 
      as (common) version (3.22.15)
    }
    import (viv.money) { 
      as (money) version (2.16.14)
    }
    import (viv.entity) { 
      as (entity) version (3.7.27)
    }
    import (viv.time) { version (2.57.40)  }
    import (viv.geo) { version (8.0.13)  }
  }
}